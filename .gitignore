# Dữ liệu thi — nặng (~1GB), đã lưu riêng ở Google Drive (xem pipeline/kaggle_pipeline.ipynb).
# Vẫn giữ lại Dataset/README.md (tài liệu, nhẹ, không nằm trong VAI_NVS_DATA/).
Dataset/VAI_NVS_DATA/

# Output trung gian của pipeline (COLMAP database, model 3DGS, ảnh render...) — sinh lại được, không phải source.
pipeline/work/

# Checkpoint (gs_model/) tải thủ công về từ Kaggle Output cho từng scene — nặng, chỉ lưu
# local/Google Drive, không lưu trong repo (cấu trúc thư mục public_set/private_set1/<scene>
# tự tạo lại bằng mkdir khi cần, xem hướng dẫn ở kaggle_private.ipynb Bước 7).
checkpoints/

# Repo train/render 3DGS — được clone lại mỗi lần chạy (xem 03_train_3dgs.sh), không lưu trong repo này.
gaussian-splatting/

# Bài nộp đóng gói / mọi file zip khác — build artifact, không phải source.
*.zip

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Môi trường ảo (nếu tạo local)
venv/
.venv/
env/

# Hệ điều hành
.DS_Store
Thumbs.db

# Phòng khi lỡ tạo file chứa secret cục bộ
.env
*.token
VAI_NVS_DATA_ROUND2/
Result/
colab_b2_results_drop/
downloads/