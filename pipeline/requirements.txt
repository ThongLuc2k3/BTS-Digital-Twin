# Cài trên máy CÓ GPU CUDA (máy local GTX 1650 4GB hiện tại KHÔNG đủ để train
# 3DGS chất lượng tốt cho 13 scene — xem KE_HOACH_VONG1.md mục 2, điểm 6).
#
# 1) PyTorch: cài theo đúng hướng dẫn https://pytorch.org/get-started/locally/
#    khớp phiên bản CUDA của máy thuê (không ghi cứng ở đây vì phụ thuộc máy).
#
# 2) pycolmap: chạy toàn bộ SfM (01_run_colmap.py, 02_validate_frame.py) mà
#    không cần cài `colmap` CLI riêng.
pycolmap>=3.10

# 3) Repo train/render 3D Gaussian Splatting: KHÔNG cài qua pip, clone riêng:
#      git clone --recursive https://github.com/graphdeco-inria/gaussian-splatting.git
#      export GS_REPO=/path/to/gaussian-splatting
#    rồi làm theo README của repo đó để cài diff_gaussian_rasterization + simple-knn
#    (2 CUDA extension bắt buộc, cần build bằng nvcc khớp bản CUDA của máy).

# 4) Đánh giá & tiện ích
numpy
Pillow
scikit-image>=0.19   # cần >=0.19 để dùng tham số channel_axis trong 05_eval_metrics.py
lpips
plyfile
tqdm
